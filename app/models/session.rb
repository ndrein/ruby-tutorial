# Session is the model for the sessions table.
# The ActiveRecord class is LearningSession (legacy name kept for backward compatibility).
# Use LearningSession directly or via this alias.
Session = LearningSession
